간단한 계산
===================

### SBKo

월드컵에 출전한 A 선수가 획득한 골을 정리하였다. 
출전하지않은 경우는 NA라고 기록하였다.  

```{r echo=FALSE}
a <- c(1, 2, 0, 1,  0, 1, NA)
a
```

이 선수는 모두 `r length(a)` 경기 중 `r sum(!is.na(a))`에 출전하여 
모두 `r sum(a, na.rm=TRUE)`개의 골을 넣었다.